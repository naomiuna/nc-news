const topicData = require('./topics');
const artcleData = require('./articles');
const userData = require('./users');
const commentData = require('./comments');

module.exports = { topicData, artcleData, userData, commentData };
